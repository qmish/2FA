## Релиз: фильтры аудита в Admin API

### Изменения
- Добавлены параметры фильтрации аудита (`actor_user_id`, `entity_type`, `action`, `from`, `to`).
- OpenAPI расширен параметрами и enum значениями.
- Документация по безопасности обновлена.

### Тестирование
- `go test ./...` — успешно.
