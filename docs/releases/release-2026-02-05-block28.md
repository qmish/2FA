## Релиз: расширение UI

### Изменения
- UI: управление токенами (refresh/logout) и просмотр текущей блокировки.
- UI: отзыв сессий по ID и revoke_all (кроме текущей).
- Сохранение access/refresh в localStorage.

### Тестирование
- `go test ./...` — успешно.
