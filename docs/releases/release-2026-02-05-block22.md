## Релиз: payload для 2FA/lockout аудита

### Изменения
- В `payload` 2FA‑аудита сохраняется метод (`otp`/`call`/`push`).
- В `payload` `lockout_create` сохраняется причина.
- Документация по безопасности обновлена.

### Тестирование
- `go test ./...` — успешно.
