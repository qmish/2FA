## Релиз: аудит админского отзыва сессий

### Изменения
- Аудит для админских операций `session_revoke` и `session_revoke_all`.
- Прокидывание `actor_user_id` администратора из handler в сервис.
- Обновление документации по безопасности.

### Тестирование
- `go test ./...` — успешно.
