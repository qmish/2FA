## Релиз: безопасный logout

### Изменения
- Logout требует bearer‑token и использует сессию из access‑token.
- Защита от отзыва чужих сессий: несовпадение `session_id` → `403`.
- OpenAPI обновлён для `auth/logout`.

### Тестирование
- `go test ./...` — успешно.
